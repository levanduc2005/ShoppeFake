import React from "react";
import { LoginComponent } from "./../Components";

function Register() {
  return (
    <div>
      <LoginComponent text="Đăng ký" login={false} />
    </div>
  );
}

export default Register;
